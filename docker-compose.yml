version: "3.9"

services:

  traefik:
    image: traefik:v3.1
    container_name: retrouve-traefik

    restart: unless-stopped

    command:
      - --api.dashboard=true

      - --providers.docker=true
      - --providers.docker.exposedbydefault=false

      - --entrypoints.web.address=:80
      - --entrypoints.websecure.address=:443

      - --certificatesresolvers.letsencrypt.acme.email=admin@retrouve-moi.com
      - --certificatesresolvers.letsencrypt.acme.storage=/letsencrypt/acme.json
      - --certificatesresolvers.letsencrypt.acme.httpchallenge=true
      - --certificatesresolvers.letsencrypt.acme.httpchallenge.entrypoint=web

      # Logging
      - --accesslog=true
      - --log.level=INFO

    ports:
      - "80:80"
      - "443:443"

    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./traefik/letsencrypt:/letsencrypt

    networks:
      - web

  ui:
    build:
      context: ./ui
      dockerfile: Dockerfile

    container_name: retrouve-ui

    restart: unless-stopped

    environment:
      NODE_ENV: production
      NEXT_TELEMETRY_DISABLED: 1
      NEXT_PUBLIC_API_URL: https://api.retrouve-moi.com

    labels:
      - traefik.enable=true

      # Router
      - traefik.http.routers.ui.rule=Host(`retrouve-moi.com`) || Host(`www.retrouve-moi.com`)
      - traefik.http.routers.ui.entrypoints=websecure
      - traefik.http.routers.ui.tls=true
      - traefik.http.routers.ui.tls.certresolver=letsencrypt

      # Service
      - traefik.http.services.ui.loadbalancer.server.port=3000

      # Compression
      - traefik.http.middlewares.compress.compress=true

      # Security headers
      - traefik.http.middlewares.secure.headers.browserXssFilter=true
      - traefik.http.middlewares.secure.headers.contentTypeNosniff=true
      - traefik.http.middlewares.secure.headers.forceSTSHeader=true
      - traefik.http.middlewares.secure.headers.stsIncludeSubdomains=true
      - traefik.http.middlewares.secure.headers.stsPreload=true
      - traefik.http.middlewares.secure.headers.stsSeconds=31536000
      - traefik.http.middlewares.secure.headers.referrerPolicy=no-referrer-when-downgrade

      # HTTPS redirect
      - traefik.http.middlewares.redirectscheme.redirectscheme.scheme=https

      # Apply middlewares
      - traefik.http.routers.ui.middlewares=compress,secure

    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:3000"]
      interval: 30s
      timeout: 10s
      retries: 3

    networks:
      - web

  nginx:
    image: nginx:1.27-alpine

    container_name: retrouve-nginx

    restart: unless-stopped

    depends_on:
      - php

    volumes:
      - ./api:/var/www
      - laravel_storage:/var/www/storage
      - ./api/docker/nginx/default.conf:/etc/nginx/conf.d/default.conf

    labels:
      - traefik.enable=true

      # Router
      - traefik.http.routers.api.rule=Host(`api.retrouve-moi.com`)
      - traefik.http.routers.api.entrypoints=websecure
      - traefik.http.routers.api.tls=true
      - traefik.http.routers.api.tls.certresolver=letsencrypt

      # Service
      - traefik.http.services.api.loadbalancer.server.port=80

      # Compression
      - traefik.http.middlewares.compress.compress=true

      # Security headers
      - traefik.http.middlewares.secure.headers.browserXssFilter=true
      - traefik.http.middlewares.secure.headers.contentTypeNosniff=true
      - traefik.http.middlewares.secure.headers.forceSTSHeader=true
      - traefik.http.middlewares.secure.headers.stsIncludeSubdomains=true
      - traefik.http.middlewares.secure.headers.stsPreload=true
      - traefik.http.middlewares.secure.headers.stsSeconds=31536000

      # Apply middlewares
      - traefik.http.routers.api.middlewares=compress,secure

    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost"]
      interval: 30s
      timeout: 10s
      retries: 3

    networks:
      - web
      - internal

  php:
    build:
      context: ./api
      dockerfile: Dockerfile

    container_name: retrouve-php

    restart: unless-stopped

    working_dir: /var/www

    environment:
      APP_ENV: production
      APP_DEBUG: false

    volumes:
      - ./api:/var/www
      - laravel_storage:/var/www/storage

    depends_on:
      - db
      - redis

    healthcheck:
      test: ["CMD", "php", "-v"]
      interval: 30s
      timeout: 10s
      retries: 3

    networks:
      - internal

  queue:
    build:
      context: ./api
      dockerfile: Dockerfile

    container_name: retrouve-queue

    restart: unless-stopped

    working_dir: /var/www

    command: php artisan queue:work redis --sleep=3 --tries=3 --timeout=90 --queue=default

    environment:
      APP_ENV: production
      APP_DEBUG: false

    volumes:
      - ./api:/var/www
      - laravel_storage:/var/www/storage

    depends_on:
      - php
      - redis

    networks:
      - internal

  scheduler:
    build:
      context: ./api
      dockerfile: Dockerfile

    container_name: retrouve-scheduler

    restart: unless-stopped

    working_dir: /var/www

    command: >
      sh -c "while true;
      do php artisan schedule:run --verbose --no-interaction;
      sleep 60;
      done"

    environment:
      APP_ENV: production
      APP_DEBUG: false

    volumes:
      - ./api:/var/www
      - laravel_storage:/var/www/storage

    depends_on:
      - php

    networks:
      - internal

  redis:
    image: redis:7-alpine

    container_name: retrouve-redis

    restart: unless-stopped

    command: redis-server --appendonly yes

    volumes:
      - redis_data:/data

    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 30s
      timeout: 10s
      retries: 3

    networks:
      - internal

  db:
    image: mysql:8.4

    container_name: retrouve-db

    restart: unless-stopped

    command:
      - --default-authentication-plugin=mysql_native_password
      - --innodb-buffer-pool-size=256M
      - --max-connections=200

    environment:
      MYSQL_DATABASE: retrouve
      MYSQL_USER: retrouve
      MYSQL_PASSWORD: strongpassword
      MYSQL_ROOT_PASSWORD: superrootpassword

    volumes:
      - mysql_data:/var/lib/mysql

    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 30s
      timeout: 10s
      retries: 5

    networks:
      - internal

volumes:
  mysql_data:
  redis_data:
  laravel_storage:

networks:
  web:
  internal:
